name: Check Branch

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - edited

permissions:
  contents: write
  pull-requests: write

jobs:
  branch-enforcer:
    uses: RockefellerArchiveCenter/.github/.github/workflows/enforcer.yml@base